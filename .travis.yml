language: ruby

rvm:
  - 1.9
  - 2.0
  - 2.1
  - 2.2
  - jruby

matrix:
  fast_finish: true
  allow_failures:
    - rvm: jruby

sudo: false

bundler_args: --without docs

# before_script:

script:
  - 'bundle install --without docs'
  - 'ruby tests.rb'

# after_script:

notifications:
  email:
    - 'ryan.parman@wepay.com'
